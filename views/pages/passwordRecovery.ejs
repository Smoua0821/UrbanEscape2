<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> - URBANESCAPE</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />

    <style>
      body {
        background: url(/images/login-bg.avif) fixed;
        background-size: cover;
        background-position: center;
      }

      .boxhandle {
        background: linear-gradient(45deg, #000000a1, #000000a1, #6daeffa1);
        padding: 20px;
        backdrop-filter: blur(5px);
        border-radius: 5px;
        border: 2px solid white;
        overflow: hidden;
        color: white;
      }

      .my-box form {
        padding: 5px;
        margin: 10px 0;
        padding-bottom: 15px;
      }

      .my-box form input.c-input {
        display: inline-block;
        width: 100%;
        margin: 5px 0;
        padding: 5px 10px;
        border: 0;
        outline: 0;
        color: #ffffffb3;
        background: #ffffff1c;
        border-bottom: 2px solid grey;
        transition: 0.1s;
        font-family: sans-serif;
        letter-spacing: 3px;
      }

      .login-box form input.c-input:focus {
        border-bottom: 2px solid white;
        color: white;
      }

      .login-box form input.c-input::placeholder {
        color: #ffffff8f;
      }
    </style>
  </head>
  <body>
    <div
      class="w-100 d-inline-flex vh-100 justify-content-center align-items-center msg-<%= type %>"
    >
      <div class="shadow-lg mx-3 boxhandle">
        <%if(locals.type=="request"){%>
        <h3>Set Password</h3>
        <%}else{%>
        <h3 class="border-bottom mb-3 pb-3">Password Recovery</h3>
        <p class="lead">Vijay Singh (thakurvijayofficial@gmail.com)</p>
        <div class="body-content">
          <div class="my-box">
            <form
              class="form m-0 p-0"
              action="/auth/password/recovery"
              method="POST"
            >
              <input
                type="password"
                class="c-input"
                name="pass1"
                placeholder="Enter New Password"
              />
              <input
                type="password"
                class="c-input"
                name="pass2"
                placeholder="Confirm new Password"
              />
              <div class="my-2">
                <div class="d-inline-flex w-100 justify-content-center">
                  <div
                    class="g-recaptcha"
                    data-sitekey="<%=CAPTCHA_KEY%>"
                    data-theme="dark"
                    data-callback="onCaptchaSuccess"
                  ></div>

                  <input type="hidden" id="captchaCode" />
                </div>
              </div>
              <button
                class="btn btn-secondary w-100 mt-2 fnlsbmt"
                style="display: none"
              >
                Submit
              </button>
            </form>
          </div>
        </div>
        <%}%>
      </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
      $(document).ready(() => {
        $("form").submit(() => {
          if (!$("#captchaCode").val()) {
            alert("Captcha is Mandatory!");
            return false;
          }
          const pass1 = $("form input[name='pass1']").val();
          const pass2 = $("form input[name='pass2']").val();
          if (!pass1 || !pass2) {
            alert("All fields are required!");
            return false;
          } else if (pass1 != pass2) {
            alert("Password must be same!");
          } else if (pass1.length < 6) {
            alert("Password length must not be less than 6!");
          }
        });
      });
      function onCaptchaSuccess(response) {
        $("#captchaCode").val(response);
        $(".fnlsbmt").show();
      }
    </script>
    <script src="https://www.google.com/recaptcha/api.js"></script>
  </body>
</html>
