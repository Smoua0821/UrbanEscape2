<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> - URBANESCAPE</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div
      class="w-100 d-inline-flex vh-100 justify-content-center align-items-center msg-<%= type %>"
    >
      <div class="shadow-lg mx-3 boxhandle">
        <h3 class="text-center py-2 border-bottom"><%=title%></h3>
        <div class="body py-2 px-2 px-sm-4">
          <p class="text-center mb-3">
            <span class="fa fa-4x fa-<%=icon%> mainicon"></span>
          </p>
          <p class="text-center lead"><%=message%></p>
        </div>
        <div class="mb-3 text-center border-top pt-3">
          <a href="/auth" class="btn btn-secondary">Login</a>
        </div>
      </div>
    </div>
    <style>
      .msg-success {
        background: linear-gradient(45deg, #00ff003b, #00700f2e);
      }

      .msg-danger {
        background: linear-gradient(45deg, #ffa5003b, #ff00002e);
      }

      .msg-danger .shadow-lg {
        border: 2px solid #ff00005c;
      }

      .msg-success .shadow-lg {
        border: 2px solid #00ff2f5c;
      }

      body {
        background: url(/images/login-bg.avif) fixed;
        background-size: cover;
        background-position: center;
      }

      .boxhandle {
        backdrop-filter: blur(10px);
        color: white;
        background: rgba(0, 0, 0, 0.7);
      }

      .msg-success .boxhandle h3 {
        color: lime;
      }

      .msg-danger .boxhandle h3 {
        color: red;
      }

      .boxhandle .mainicon {
        padding: 20px;
        color: #fff;
        border-radius: 50%;
        border: 2px solid #ffffff80;
      }
    </style>
  </body>
</html>
